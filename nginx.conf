server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
        expires -1;
    }
    location /api/ {
        # todo: 改成正确的ip
        proxy_pass http://10.1.51.33:3000/;
    }
}